name: formularioia
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5       # Para gestión del estado
  http: ^1.0.0           # Para las llamadas a la API
  flutter_dotenv: ^5.0.2 # Para manejar variables de entorno
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Declara los assets aquí
  assets:
    - .env
